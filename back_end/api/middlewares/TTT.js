const pool = require('../connect_db');
const { checkRequireField } = require('../utils/checkHelper');
const { callAndCatchApiSuccess } = require('../utils/fakeHelper');

// search
async function searchTTT (req, res, next) {
    return;
}

// insert
async function insertTTT (req, res, next) {
    return;
}

// update
async function updateTTT(req, res, next) {
    return;
}

// delete
async function deleteTTT(req, res, next) {
    return;
}

module.exports = {
    searchTTT,
    insertTTT,
    updateTTT,
    deleteTTT
}