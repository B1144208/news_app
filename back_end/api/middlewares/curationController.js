const pool = require('../connect_db');
const { checkRequireField } = require('../utils/checkHelper');
const { callAndCatchApiSuccess } = require('../utils/fakeHelper');

// search
async function eventsortingCuration (req, res, next) {
    return;
}

// insert
async function multipleperspectivesCuration (req, res, next) {
    return;
}

module.exports = {
    eventsortingCuration,
    multipleperspectivesCuration
}